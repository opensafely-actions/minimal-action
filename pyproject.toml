[project]
name = "minimal-action"
description = "Minimal action for testing actions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

dynamic = ["version"]

[dependency-groups]
dev = [
    "ruff",
]
